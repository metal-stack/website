---
slug: /references/api
title: API
---

import { useState, useEffect } from "react";

export function VersionedLink({ children }) {
  const [version, setVersion] = useState("");

  useEffect(() => {
    const path = window.location.pathname;
    const v = path
      .replace("/docs/", "")
      .replace("references/api", "")
      .replace("/", "");
    if (v === "") setVersion("latest");
    else setVersion(v);
  }, []);

  if (version) return <a href={`/api?version=${version}`}>{children}</a>;

  return null;
}

Here you can access our API documentation pages.

| Version | Repository                                    | Docs                                        |
| ------- | --------------------------------------------- | ------------------------------------------- |
| v1      | [metal-api](https://github.com/metal-stack/metal-api) | <VersionedLink>Spec</VersionedLink> |
| v2      | [api](https://github.com/metal-stack/api)             | Comming soon                        |
